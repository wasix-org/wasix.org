import { Callout } from "nextra-theme-docs";

Wasix for C has a very easy to use layer (similar to emscripten).

<Callout type="info" emoji="ℹ️">
  Wasix for C dependes on `python3` to be available in your system.
</Callout>

```console copy
$ curl https://raw.githubusercontent.com/wasmer/wasix/master/install.sh | sh
```

#### Verifying the Installation

<Callout type="warning" emoji="🚨">
  Todo: Verify the C installation
</Callout>

<Callout type="info" emoji="ℹ️">
  You can also build **Wasix from source** if you prefer that to downloading
  binaries.
</Callout>
