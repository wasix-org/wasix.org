import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  'file-copy': 'File Copy',
  longjmp: 'Longjmp',
  readdir: 'Readdir',
  signal: 'Signal',
  spawn: 'Spawn'
};

export default meta;
